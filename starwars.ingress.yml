apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myingress
  namespace: backend
spec:
  ingressClassName: nginx
  rules:
    - host: billytest.hopto.org
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: starwars
                port:
                  number: 3000
